name: lumna

volumes:
  reports_postgres_data:

services:
  # postgres:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_DB: postgres
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #   ports:
  #     - 5432:5432
  #   volumes:
  #     - lumna_postgres_data:/var/lib/postgresql/data/

  mailpit:
    image: axllent/mailpit:latest
    restart: always
    ports:
      - 1025:1025
      - 8025:8025
    environment:
      MP_SMTP_AUTH_ACCEPT_ANY: 1
      MP_SMTP_AUTH_ALLOW_INSECURE: 1
