import { createAction } from "@ngrx/store";

export const actionApplicationInit = createAction("[Application] Init");
export const actionApplicationSidebarToggle = createAction(
    "[Application] Sidebar toggle"
);
