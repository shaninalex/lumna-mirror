package templates

import "gitlab.com/shaninalex/lumna/app/web/pages/templates/partials"

templ Base(title string) {
	<!DOCTYPE html>
	<html lang="en">
		<head>
			@partials.Head(title)
		</head>
		<body>
			{ children... }

			@partials.FooterIncludes()
		</body>
	</html>
}
