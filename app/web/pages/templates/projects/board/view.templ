package board

import "context"
import "gitlab.com/shaninalex/lumna/app/web/pages/templates"

templ View(ctx context.Context, projectID, boardID string) {
	@templates.Dashboard(ctx, "Board") {
		<div class="board-view">
			<h1>Kanban Board</h1>
		</div>
	}
}
