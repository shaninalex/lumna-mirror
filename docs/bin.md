# /bin

Contains scripts that simplifies developing:

- `./bin/create_user.sh` - create user in database
- `./bin/dev.sh` - start dev server
- `./bin/migrate.sh` - apply migrations and create local sqlite database
- `./bin/release.sh` - bump new tag
- `./bin/clear_port.sh` - clear port if application stoped but port still used
