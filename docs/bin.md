# /bin

Contains scripts that simplifies developing:

- `./bin/create_user` - create user in database
- `./bin/dev.sh` - start dev server
- `./bin/migrate.sh` - apply migrations and create local sqlite database
